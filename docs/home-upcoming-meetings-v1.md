# Home upcoming meetings v1

The Home `Ближайшее` card now renders upcoming meetings as a compact list. Long titles are limited to two lines, date/time and project metadata use secondary typography, and multiple meetings scroll inside the card instead of stretching the page.
