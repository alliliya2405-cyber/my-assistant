# Project State
## Текущее состояние проекта «Мой ассистент»

---

## Последнее обновление

Дата: 10.08.2026

---

# 1. Что это за проект

«Мой ассистент» — персональное веб-приложение для организации задач и дальнейшего развития функций личного цифрового ассистента.

Проект развивается небольшими законченными этапами без переписывания работающих частей без необходимости.

---

# 2. Стабильная версия

Стабильная контрольная версия находится в ветке `main`.

В `main` находятся:

- Architecture v2;
- единая модель задачи `core/models/task.js`;
- Data Model v1;
- Data Store Design v1;
- минимальный `core/storage/data-store.js`;
- подключение Data Store к начальному чтению состояния приложения.

---

# 3. Что сделано на последнем этапе

Этап выполнен в ветке `data-store-load-v1` и объединён с `main` через Pull Request #5.

В `index.html`:

- `core/storage/data-store.js` подключён до `app.js`;
- cache-busting параметр `app.js` обновлён до `v=17`.

В `app.js` изменена только начальная загрузка состояния:

- прямое чтение `localStorage` заменено на `MyAssistantDataStore.load()`;
- существующие `normalize(...)`, `sample()` и `try/catch` сохранены.

`persist(...)`, импорт, экспорт и остальные операции хранения не менялись.

---

# 4. Что проверено

Перед объединением выполнена самопроверка итогового diff.

Подтверждено:

- в рабочем коде изменена одна строка `app.js`;
- `data-store.js` загружается до `app.js`;
- старый cache-busting `app.js?v=16` заменён на `v=17`;
- пустое хранилище сохраняет прежнее поведение через `normalize(null)`;
- повреждённый JSON сохраняет прежнее безопасное поведение через существующий `catch` и `sample()`;
- `persist(...)` продолжает писать напрямую в прежний `localStorage`;
- ключ `myAssistantProFinal.v1` не изменён;
- формат данных не менялся;
- миграция и очистка пользовательских данных не выполнялись;
- временные технические файлы, использованные при точечном изменении большого `app.js`, удалены и в итоговый diff не вошли.

Pull Request #5 имел состояние `mergeable: true`, `mergeable_state: clean` и успешно объединён с `main`.

---

# 5. Принятые решения

- Data Store подключается постепенно, небольшими проверяемыми шагами.
- Начальное чтение уже переведено на Data Store.
- Сохранение через `persist(...)` пока не переводится на Data Store до проверки опубликованной версии.
- Импорт, экспорт и резервные копии пока не меняются.
- `normalize(...)`, `sample()`, модель задач и формат JSON остаются прежними.

---

# 6. Над чем работаем сейчас

Изменение начального чтения находится в `main`.

Следующая контрольная задача — проверить опубликованную GitHub Pages версию и убедиться, что существующие пользовательские данные открываются без изменений.

---

# 7. Следующее одно действие

**проверить публикацию GitHub Pages после Pull Request #5 и затем выполнить пользовательскую проверку открытия существующих данных.**

Только после успешной проверки переходить к переводу `persist(...)` на `MyAssistantDataStore.save(state)`.

---

# 8. Что пока не менять

Пока не менять:

- интерфейс приложения;
- `persist(...)`;
- импорт и экспорт;
- ключ `myAssistantProFinal.v1`;
- формат `localStorage`;
- `normalize(...)`;
- `sample()`;
- `core/models/task.js`;
- пользовательские данные;
- способ хостинга.

---

# 9. Что отложено на потом

После успешной проверки опубликованной версии отдельно выполнять:

- перевод `persist(...)` на `MyAssistantDataStore.save(state)`;
- проверку сохранения после перезагрузки;
- перевод резервной копии импорта на Data Store;
- проверку импорта и восстановления резервной копии;
- дальнейшее развитие слоя данных только по необходимости.

---

# Контрольная точка

Стабильная версия:

**Architecture v2 + Data Model v1 + Data Store Design v1 + Data Store v1 + Data Store initial load v1 находятся в `main`.**

Текущий статус:

**начальное чтение состояния уже идёт через Data Store; сохранение и импорт пока работают по прежней схеме.**

Следующий шаг:

**проверить GitHub Pages и существующие пользовательские данные.**
