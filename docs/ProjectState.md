# Project State
## Текущее состояние проекта «Мой ассистент»

---

## Последнее обновление

Дата: 11.08.2026

---

# 1. Что это за проект

«Мой ассистент» — персональный цифровой планнер и цифровой ассистент для ежедневной работы с задачами, проектами, планированием, знаниями и личными областями жизни.

Проект развивается небольшими законченными этапами с обязательной визуальной проверкой и защитой пользовательских данных.

---

# 2. Стабильная версия

Стабильная контрольная версия находится в ветке `main`.

В `main` находятся Architecture v2, Data Model v1, Data Store v1, Application UX navigation v1, основные этапы дизайн-системы для Главной, задач, проектов и планирования, завершённые визуальные этапы разделов «Жизнь» и «Знания».

Последние подтверждённые этапы раздела «Знания»:

- Notes Cards v1 + Notes Collapse Fix v1;
- Library Cards v1 + усиленный Library guard;
- Reflection Cards v1 + Reflection Collapse Fix v2;
- Board + Quotes v1 через PR #37.

---

# 3. Что завершено и проверено

Основные экраны «Главная», «Задачи», «Проекты», «Сегодня», «Неделя» и «Месяц» проверены пользователем на опубликованной версии.

Раздел «Жизнь» завершён и визуально подтверждён пользователем.

Раздел «Знания» завершён и визуально подтверждён пользователем:

- «Заметки» — чистая раскрытая карточка без универсального collapse;
- «Библиотека» — карточка материала без лишней collapse-шапки;
- «Рефлексия» — одна карточка без дублирующей шапки, «Следующий шаг» выделен;
- «Доска» — панель инструментов и рабочее поле отображаются корректно;
- «Цитаты дня» — статистика, фильтры и карточки имеют ясную иерархию, лишнего collapse нет.

При визуальных этапах пользовательские данные, Data Store и бизнес-логика не менялись.

---

# 4. Принятые решения

- исправлять подтверждённую причину, а не маскировать симптом;
- избегать вложенной вертикальной прокрутки там, где экран может расти в обычном потоке страницы;
- не дублировать существующую бизнес-логику;
- универсальный collapse не должен скрывать ключевые пользовательские карточки;
- текст и содержимое записи важнее технических действий;
- данные и Data Store не менять для чисто визуальных этапов;
- после завершения основных пользовательских разделов перейти к служебному блоку «Ещё».

---

# 5. Над чем работаем сейчас

Разделы «Жизнь» и «Знания» завершены и визуально проверены.

Начат служебный блок **«Ещё»**.

Текущий этап — **«Ещё → Поиск»**: улучшить визуальную иерархию строки глобального поиска и результатов без изменения алгоритма поиска.

Рабочая ветка: `more-search-v1`.

---

# 6. Следующее одно действие

**объединить Search View v1, открыть «Ещё → Поиск» и визуально проверить строку поиска, пустое состояние и карточки результатов.**

---

# 7. Что пока не менять

Без отдельной причины не менять:

- пользовательские данные;
- Data Store и формат `localStorage`;
- `core/models/task.js`;
- календарную и проектную бизнес-логику;
- автоматические связанные задачи;
- route id;
- алгоритм глобального поиска;
- импорт и экспорт;
- уже принятые экраны планирования, проектов, «Жизни» и «Знаний».

---

# 8. Что отложено на потом

После проверки «Поиска» последовательно пройти остальные служебные экраны блока «Ещё».

Отдельно позже решить перенос импорта/экспорта из footer в настройки.

Сервер и синхронизацию между устройствами рассматривать только при реальной потребности.

---

# Контрольная точка

Стабильная версия:

**основные экраны планирования, проекты, весь раздел «Жизнь» и весь раздел «Знания» визуально проверены.**

Текущий этап:

**Search View v1 в рабочей ветке `more-search-v1`.**

Следующий шаг:

**объединить безопасный UI-этап и проверить опубликованный экран «Ещё → Поиск».**
