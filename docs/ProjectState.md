# Project State
## Текущее состояние проекта «Мой ассистент»

---

## Последнее обновление

Дата: 15.08.2026

---

# 1. Что это за проект

«Мой ассистент» — персональный цифровой планнер и цифровой ассистент для ежедневной работы с задачами, проектами, планированием, знаниями и личными областями жизни.

Проект развивается небольшими законченными этапами с обязательной проверкой и защитой пользовательских данных.

---

# 2. Стабильная версия

Стабильная контрольная версия находится в ветке `main`.

---

# 3. Что завершено и проверено

- основные экраны планирования доведены до единой спокойной визуальной системы;
- раздел «Жизнь» завершён и подтверждён пользователем;
- раздел «Знания» завершён и подтверждён пользователем;
- служебный блок «Ещё» прошёл автономный UI-проход;
- раздел «Проекты» прошёл общий UX/UI-проход через PR #39;
- пошаговая UX/UI-доработка «Проекты → Совещания» завершена.

Для «Совещаний» визуально подтверждены Meeting Cards Collapse v1, Meetings Reading Hierarchy v1 и Meetings Empty Sections v1.

---

# 4. Принятые решения

- раздел «Проекты» дорабатывать по одному экрану и принимать каждый шаг отдельно;
- исправлять причину, а не маскировать симптом;
- данные и Data Store не менять для чисто визуальных этапов;
- не продолжать полировку уже удобного экрана без конкретной пользовательской проблемы;
- на широком экране списка проектов название и ключевые метаданные проекта должны визуально оставаться рядом с управляющим элементом карточки, а не теряться в центре большой пустой области.

---

# 5. Над чем работаем сейчас

Текущий рабочий экран — **«Проекты → Все проекты»**.

По пользовательскому скриншоту выполнена первичная UX/UI-оценка. Выбран первый небольшой этап — **All Projects Scanability v1**.

В `main` внесено только визуальное изменение `styles/project-cards-v1.css`:

- в свёрнутых карточках на широком экране ограничена ширина смыслового блока проекта;
- название, категории и метаданные визуально привязаны ближе к кнопке раскрытия;
- правая группа действий остаётся отдельной и доступной;
- на экранах до 1100 px ограничение снимается, чтобы не ухудшать адаптивность;
- бизнес-логика, данные, Data Store и обработчики проектов не менялись.

Версия stylesheet в `index.html` обновлена до `project-cards-v1.css?v=2` для надёжной публикации без старого кэша.

Рабочая версия: `main`.

---

# 6. Следующее одно действие

**после публикации открыть «Проекты → Все проекты» и визуально проверить, что на широком экране названия и метаданные проектов читаются ближе к левому краю карточки и список легче сканировать, при этом кнопки действий остаются справа.**

---

# 7. Что пока не менять

Без отдельной продуктовой причины не менять:

- пользовательские данные;
- Data Store и формат `localStorage`;
- `core/models/task.js`;
- проектную и календарную бизнес-логику;
- автоматические связанные задачи;
- route id;
- завершённый экран «Совещания»;
- экраны «Команда», «Ресурсная карта» и «Отчёты» до их отдельного этапа.

---

# 8. Что отложено на потом

После «Все проекты» отдельно перейти к «Команда», «Ресурсная карта» и «Отчёты».

Сервер и синхронизацию между устройствами рассматривать только при реальной потребности.

---

# Контрольная точка

Стабильная версия: `main`.

Экран «Совещания» завершён.

Текущий этап: **All Projects Scanability v1 — изменение в `main`, ожидает визуальной проверки опубликованной версии**.

Следующий шаг: **проверить обновлённый список «Все проекты» на широком экране.**
