# Project State
## Текущее состояние проекта «Мой ассистент»

---

## Последнее обновление

Дата: 15.08.2026

---

# 1. Что это за проект

«Мой ассистент» — персональный цифровой планнер и цифровой ассистент для ежедневной работы с задачами, проектами, планированием, знаниями и личными областями жизни.

Проект развивается небольшими законченными этапами с обязательной проверкой и защитой пользовательских данных.

---

# 2. Стабильная версия

Стабильная контрольная версия находится в ветке `main`.

---

# 3. Что завершено и проверено

- основные экраны планирования доведены до единой спокойной визуальной системы;
- раздел «Жизнь» завершён и подтверждён пользователем;
- раздел «Знания» завершён и подтверждён пользователем;
- служебный блок «Ещё» прошёл автономный UI-проход;
- раздел **«Проекты» пошагово завершён целиком**;
- экран «Проекты → Все проекты» завершён;
- экран «Проекты → Совещания» завершён;
- экран «Проекты → Команда» завершён;
- экран «Проекты → Ресурсная карта» завершён;
- экран «Проекты → Отчёты» завершён;
- подтверждены отдельные этапы Team Actions Hierarchy v1, Team Card Hierarchy v1, Assignment Modal Compact v1, Resource Map Summary Density v1, Resource Person Hierarchy v1, Reports Workflow Hierarchy v1 и Reports Document Reading Hierarchy v1;
- Team Project Linkage Audit завершён: отдельное исправление логики не требуется;
- **Home Daily Focus v1** визуально подтверждён пользовательским скриншотом 15.08.2026;
- начат **Final Product Audit v1**;
- добавлен общий GitHub Actions workflow `.github/workflows/release-regression.yml`, который на `main` и PR проверяет синтаксис JavaScript, запускает все `tests/*.test.js` и проверяет существование локальных CSS/JS-файлов, подключённых из `index.html`;
- общий regression run выявил устаревшую слишком жёсткую проверку `reports-document-readability-v1.test.js`; тест исправлен без изменения поведения продукта;
- общий regression run затем выявил реальный дефект публикации: `index.html` ссылался на отсутствующий `styles/leisure-view-v1.css`, хотя фактический подтверждённый файл называется `styles/leisure-cards-v1.css`;
- ссылка на CSS «Досуга» восстановлена;
- проведён отдельный аудит сохранения, экспорта и импорта данных;
- экспорт подтверждён как полный JSON-снимок текущего `state`;
- импорт до аудита принимал любой JSON-объект и мог нормализовать посторонний объект в новое состояние, что создавало риск незаметной замены пользовательских данных;
- добавлен `fixes/backup-import-guard-v1.js`: импорт теперь требует структуру резервной копии «Моего ассистента» до любых изменений состояния;
- при неправильном JSON данные не изменяются и пользователь получает явное сообщение;
- перед корректным импортом по-прежнему создаётся резервная копия `myAssistantProFinal.v1.beforeImport`;
- добавлен regression test `tests/backup-import-guard-v1.test.js`;
- после исправлений общий release regression успешно проходит: синтаксис JavaScript — OK, **10 regression test files — OK**, все локальные ресурсы `index.html` существуют;
- пользовательский smoke-test «Жизнь → Досуг» подтверждён скриншотом 15.08.2026: карточка снова занимает одну колонку двухколоночной сетки на широком экране, стили загружаются корректно, действия «Изменить/Удалить» и метаданные читаются нормально.

---

# 4. Принятые решения

- проект развивается по одному небольшому завершённому шагу;
- исправлять причину, а не маскировать симптом;
- данные и Data Store не менять для чисто визуальных этапов;
- не продолжать полировку уже удобного состояния без конкретной пользовательской проблемы;
- завершённые экраны больше не менять без отдельной причины;
- перед фиксацией первой итоговой версии выполнить **Final Product Audit v1**, а не начинать новый цикл косметической доработки;
- релизный аудит включает автоматическую проверку синтаксиса, всех существующих регрессионных тестов и подключаемых ресурсов;
- найденные в финальном аудите дефекты исправлять минимально и сразу повторно прогонять общий regression workflow;
- импорт полной резервной копии не должен изменять данные до подтверждения, что файл имеет ожидаемую структуру приложения;
- пользовательский smoke-test исправленного «Досуга» считается пройденным;
- сервер и синхронизация между устройствами не являются условием первой итоговой версии.

---

# 5. Над чем работаем сейчас

Текущая стадия — **предрелизная проверка / Final Product Audit v1**.

Техническая часть предрелизного аудита завершена, первый пользовательский smoke-test также пройден:

- создан единый release regression workflow;
- найдена и исправлена устаревшая проверка теста;
- найден и исправлен отсутствующий CSS-ресурс «Досуга»;
- проверена логика `localStorage`, экспорта и полного импорта;
- устранён риск замены данных произвольным JSON-файлом;
- текущий общий regression workflow на `main` проходит полностью;
- опубликованный «Досуг» визуально подтверждён пользователем после исправления CSS-ссылки.

Рабочая версия: `main`.

На этом этапе новых косметических улучшений не добавляем.

---

# 6. Следующее одно действие

**провести финальную пользовательскую проверку резервного экспорта: нажать «Экспорт», убедиться, что браузер скачивает JSON-файл резервной копии `my-assistant-backup-YYYY-MM-DD.json`, и сохранить его как контрольную копию перед фиксацией первой итоговой версии.**

После подтверждения этого шага можно фиксировать первую итоговую стабильную версию продукта.

---

# 7. Что пока не менять

Без отдельной найденной в финальном аудите причины не менять:

- пользовательские данные;
- формат `localStorage`;
- `core/models/task.js`;
- основную бизнес-логику `app.js`;
- проектную и календарную бизнес-логику;
- автоматические связанные задачи;
- route id;
- завершённые экраны раздела «Проекты»;
- подтверждённую Главную;
- структуру данных отчётов;
- генерацию Word;
- правила формирования сводного документа отдела;
- расчёт и проверку 100% распределения времени;
- разделы «Жизнь», «Знания» и «Ещё», кроме дефектов, найденных финальным аудитом.

---

# 8. Что отложено на потом

- сервер;
- синхронизация между устройствами;
- новая функциональность после фиксации первой итоговой версии.

---

# Контрольная точка

Стабильная версия: `main`.

Завершённые крупные области:

- **«Проекты» — завершён целиком**;
- **«Главная» — завершена на текущем уровне**;
- **«Жизнь» — завершён**;
- **«Знания» — завершён**;
- **«Ещё» — прошёл UI-проход**.

Текущая стадия: **Final Product Audit v1**.

Последний подтверждённый результат: **«Жизнь → Досуг» успешно прошёл пользовательский smoke-test после восстановления корректной CSS-ссылки**.

Следующий шаг: **проверка скачивания резервной копии через «Экспорт».**
