# Project State
## Текущее состояние проекта «Мой ассистент»

---

## Последнее обновление

Дата: 11.08.2026

---

# 1. Что это за проект

«Мой ассистент» — персональный цифровой планнер и цифровой ассистент для ежедневной работы с задачами, проектами, планированием, знаниями и личными областями жизни.

Проект развивается небольшими законченными этапами с обязательной проверкой и защитой пользовательских данных.

---

# 2. Стабильная версия

Стабильная контрольная версия находится в ветке `main`.

В `main` находятся:

- Architecture v2;
- единая модель задачи `core/models/task.js`;
- Data Model v1;
- Data Store v1;
- Application UX navigation v1;
- Главная / Сегодня v1;
- Design System v1 — task density;
- Task Card Unified v1 после Pull Request #10;
- Top Bar & Quick Actions v1 после Pull Request #11;
- Project Cards v1 после Pull Request #12;
- Project Scroll Fix v1 после Pull Request #13;
- Planning / Today Density v1 после Pull Request #14;
- Planning / Week Density v1 после Pull Request #15;
- документация UX и дизайн-системы в `docs/`.

---

# 3. Что завершено и проверено

Data Store проверен на реальных пользовательских данных.

Application UX navigation v1, Главная / Сегодня v1, Task Card Unified v1, Top Bar & Quick Actions v1, экран «Проекты» и Planning / Today Density v1 опубликованы и визуально подтверждены пользователем.

Planning / Week Density v1 выполнен в отдельной ветке `week-density-v1` и объединён с `main` через Pull Request #15.

На этапе Planning / Week Density v1:

- недельные итоги стали компактными информационными плашками;
- вкладки семи дней уменьшены по высоте;
- заголовок выбранного дня стал плотнее;
- задачи выбранного дня начинаются выше на экране;
- все недельные итоги и все семь дней остаются доступными;
- `app.js`, `renderWeek()`, данные задач, Data Store, модели, маршруты и обработчики не менялись;
- изменение изолировано в `styles/week-density-v1.css` и одной строке подключения в `index.html`;
- решение зафиксировано в `docs/WeekDensityV1.md`.

Pull Request #15 перед объединением имел `mergeable: true` и успешно объединён с `main`.

---

# 4. Принятые решения

- UX меняется постепенно без удаления существующих функций;
- дизайн-система развивается от плотности, читаемости и иерархии;
- визуальные дефекты после пользовательской проверки исправляются до перехода к следующему этапу;
- route id, пользовательские данные, Data Store и бизнес-логика сохраняются, пока нет отдельной причины их менять;
- недельные метрики и навигация должны помогать ориентироваться, но не отодвигать задачи выбранного дня вниз;
- следующий переход возможен после визуальной проверки опубликованной «Недели».

---

# 5. Над чем работаем сейчас

Planning / Week Density v1 уже находится в `main`.

Текущая контрольная задача — визуально проверить опубликованный экран «Планирование → Неделя»: недельные итоги и семь дней должны оставаться читаемыми, а задачи выбранного дня должны начинаться заметно выше.

---

# 6. Следующее одно действие

**после публикации GitHub Pages открыть «Планирование → Неделя» и визуально проверить верх недели и задачи выбранного дня.**

Если проверка успешна, Planning / Week Density v1 считается завершённым и можно переходить к экрану «Планирование → Месяц».

---

# 7. Что пока не менять

До пользовательской проверки Planning / Week Density v1 не менять без отдельной причины:

- пользовательские данные;
- Data Store и формат `localStorage`;
- `core/models/task.js`;
- route id;
- бизнес-логику задач, проектов и календарей;
- импорт и экспорт;
- уже принятые экраны «Сегодня» и «Проекты»;
- другие экраны массовыми изменениями;
- способ хостинга.

---

# 8. Что отложено на потом

После успешной проверки «Недели»:

- экран «Планирование → Месяц»;
- распространение дизайн-системы на остальные основные экраны;
- отдельное решение по переносу импорта/экспорта из постоянного footer в настройки;
- при необходимости улучшение глобального поиска и сервисного меню.

Отдельно по необходимости:

- перевод резервной копии импорта на Data Store;
- проверка импорта и восстановления;
- сервер и синхронизация между устройствами только при появлении реальной потребности.

---

# Контрольная точка

Стабильная версия:

**Architecture v2 + Data Model v1 + Data Store v1 + Application UX navigation v1 + Home Today dashboard v1 + Design System v1 task density + Task Card Unified v1 + Top Bar & Quick Actions v1 + Project Cards v1 + Project Scroll Fix v1 + Planning / Today Density v1 + Planning / Week Density v1 находятся в `main`.**

Текущий этап:

**пользовательская визуальная проверка Planning / Week Density v1.**

Следующий шаг:

**проверить опубликованный экран «Планирование → Неделя»; затем перейти к «Месяцу».**
