# Project State
## Текущее состояние проекта «Мой ассистент»

---

## Последнее обновление

Дата: 16.08.2026

---

# 1. Что это за проект

«Мой ассистент» — персональный цифровой планнер и цифровой ассистент для ежедневной работы с задачами, проектами, планированием, знаниями и личными областями жизни.

Проект развивается небольшими законченными этапами с обязательной проверкой и защитой пользовательских данных.

---

# 2. Стабильная версия

Первая итоговая стабильная версия продукта зафиксирована отдельной контрольной веткой **`stable-v1`** на коммите `efa2b1c02697a7280af831d41acd56a1cfea2355`.

Текущая развиваемая версия находится в ветке `main`.

---

# 3. Что завершено и проверено

- разделы «Проекты», «Главная», «Жизнь», «Знания» и служебный блок «Ещё» прошли согласованные этапы проверки;
- **Final Product Audit v1** завершён;
- резервный экспорт JSON подтверждён пользователем;
- первая итоговая версия признана пригодной для ежедневной эксплуатации;
- контрольная ветка `stable-v1` сохранена отдельно от дальнейшей разработки;
- после начала эксплуатации реализован **Task Delete Controls v1** через PR #65;
- выполнен **Task Filters & Cleanup v2** через PR #66: фильтр «По проектам / Образование / Здоровье», удаление задач в Kanban, очистка «Ходьба / Вода / Капли в глаза»;
- автоматически связанные задачи (`generatedLinked`) защищены от прямого удаления;
- массовые удаления учитывают только фактически показанные пользователю задачи;
- **Project Sprint Section Hide v1** реализован через PR #67 и объединён с `main` после успешного Release regression audit;
- скрытие блока «Спринты проекта» не удаляет данные и может быть отменено;
- **Kanban Delete All v1** реализован через PR #68 и объединён с `main` после успешного Release regression audit;
- после пользовательской проверки выявлено, что в режиме списка «Удалить всё» оставалось только в разделе «Позже»;
- **List Delete All All Groups v1** реализован через PR #69: кнопка «Удалить всё» теперь добавляется в каждый непустой раздел списка, включая «Выполнено»;
- массовое удаление в списке использует только видимые строки текущего раздела, учитывает фильтры, защищает `generatedLinked` и требует подтверждение;
- cache-busting для task delete controls поднят до `v=4`;
- PR #69 прошёл полный Release regression audit: JavaScript syntax, regression tests и проверка локальных ресурсов — success;
- PR #69 объединён с `main`, merge commit `754f7ca85c1b908bf7290c39d968c6b2b273a0ca`.

---

# 4. Принятые решения

- `stable-v1` остаётся неподвижной контрольной точкой первой стабильной версии;
- дальнейшая разработка идёт в `main` только по реальным проблемам и потребностям эксплуатации;
- пользовательские данные и формат существующего `localStorage` без отдельной необходимости не мигрировать;
- потенциально разрушительные массовые действия всегда требуют явного подтверждения;
- «Удалить всё» должно быть доступно во всех непустых группах списка и Kanban, но удалять только видимые пользователю задачи;
- автоматически связанные задачи не удалять напрямую;
- фильтры областей должны работать и в списке, и в Kanban;
- скрытие блока «Спринты проекта» не должно удалять данные проекта или подпроектов;
- по подтверждённому запросу пользователя задачи «Ходьба», «Вода», «Капли в глаза» не должны регенерироваться после удаления.

---

# 5. Над чем работаем сейчас

Текущий этап — **эксплуатация стабильной v1 + точечные улучшения по фактической необходимости**.

Последнее исправление: **List Delete All All Groups v1** — объединено с `main` после успешной автоматической проверки.

Рабочая версия: `main`.

---

# 6. Следующее одно действие

**после публикации GitHub Pages обновить экран «Задачи» в режиме списка и визуально проверить, что справа от заголовка «Выполнено» появилась кнопка «Удалить всё».**

Не нажимать её для проверки, если выполненные задачи нужно сохранить.

---

# 7. Что пока не менять

Без конкретной эксплуатационной причины не менять:

- контрольную ветку `stable-v1`;
- пользовательские данные массово, кроме явно подтверждённых пользователем операций;
- ключ хранения `myAssistantProFinal.v1` и формат основной структуры `localStorage`;
- `core/models/task.js`;
- основную бизнес-логику проектов, совещаний, отчётов и связанных задач;
- завершённые экраны только ради косметической полировки;
- сервер и синхронизацию между устройствами до появления реальной необходимости.

---

# 8. Что отложено на потом

- сервер;
- защищённая база данных;
- синхронизация между устройствами;
- новая функциональность, которая не возникла из реального сценария использования.

---

# Контрольная точка

Первая стабильная версия: **`stable-v1` → `efa2b1c02697a7280af831d41acd56a1cfea2355`**.

Текущая развиваемая версия: **`main`**.

Последний этап: **List Delete All All Groups v1 — объединён с `main`, автоматические проверки пройдены, ожидается только визуальная проверка опубликованной версии.**
