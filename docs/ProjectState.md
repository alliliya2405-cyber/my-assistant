# Project State
## Текущее состояние проекта «Мой ассистент»

---

## Последнее обновление

Дата: 11.08.2026

---

# 1. Что это за проект

«Мой ассистент» — персональный цифровой планнер и цифровой ассистент для ежедневной работы с задачами, проектами, планированием, знаниями и личными областями жизни.

Проект развивается небольшими законченными этапами с обязательной проверкой и защитой пользовательских данных.

---

# 2. Стабильная версия

Стабильная контрольная версия находится в ветке `main`.

В `main` находятся:

- Architecture v2;
- единая модель задачи `core/models/task.js`;
- Data Model v1;
- Data Store v1;
- Application UX navigation v1;
- Главная / Сегодня v1;
- Design System v1 — task density;
- Task Card Unified v1 после Pull Request #10;
- Top Bar & Quick Actions v1 после Pull Request #11;
- Project Cards v1 после Pull Request #12;
- документация UX и дизайн-системы в `docs/`.

---

# 3. Что завершено и проверено

Data Store проверен на реальных пользовательских данных: существующие и новые/изменённые задачи сохраняются после перезагрузки.

Application UX navigation v1 и Главная / Сегодня v1 опубликованы и визуально подтверждены пользователем.

Design System v1 — task density объединён с `main` через Pull Request #9.

Task Card Unified v1 объединён с `main` через Pull Request #10 и визуально проверен пользователем: раскрытая задача выглядит как единая карточка, редактирование открывается корректно.

Top Bar & Quick Actions v1 объединён с `main` через Pull Request #11 и визуально проверен пользователем: окно «Что добавить?» открывается корректно, существующая логика быстрого добавления сохранена.

Project Cards v1 выполнен в отдельной ветке `project-cards-v1` и объединён с `main` через Pull Request #12.

На этапе Project Cards v1:

- карточки проектов стали компактнее;
- название, статус, сроки и прогресс получили больший визуальный приоритет;
- служебные кнопки уменьшены;
- дублирующая кнопка «Свернуть проект» скрыта, потому что ту же функцию выполняет стрелка раскрытия;
- удаление визуально ослаблено, но остаётся доступным;
- мобильная компоновка действий улучшена;
- `app.js`, данные проектов и задач, Data Store, модели, маршруты и бизнес-логика не менялись;
- изменение изолировано в `styles/project-cards-v1.css` и одной строке подключения в `index.html`;
- проект решения зафиксирован в `docs/ProjectCardsV1.md`.

Pull Request #12 перед объединением имел `mergeable: true` и успешно объединён с `main`.

---

# 4. Принятые решения

- UX меняется постепенно без удаления существующих функций;
- дизайн-система развивается от плотности, читаемости и иерархии;
- визуальные дефекты после пользовательской проверки исправляются до перехода к следующему этапу;
- route id, пользовательские данные, Data Store и бизнес-логика сохраняются, пока нет отдельной причины их менять;
- глобальное «Быстро добавить» дополняет, а не заменяет локальные действия конкретного экрана;
- проектные карточки должны сначала показывать содержание проекта, а служебные действия — оставаться вторичными.

---

# 5. Над чем работаем сейчас

Project Cards v1 уже находится в `main`.

Текущая контрольная задача — визуально проверить опубликованный экран «Проекты»: компактность карточек, читаемость названия и метаданных, работу стрелки раскрытия и доступность действий «Фокус», «Изменить», «Удалить».

---

# 6. Следующее одно действие

**после публикации GitHub Pages открыть экран «Проекты» и визуально проверить одну свернутую и одну раскрытую карточку проекта.**

Если проверка успешна, Project Cards v1 считается полностью проверенным и можно выбирать следующий ключевой экран для распространения дизайн-системы.

---

# 7. Что пока не менять

До пользовательской проверки Project Cards v1 не менять:

- пользовательские данные;
- Data Store и формат `localStorage`;
- `core/models/task.js`;
- route id;
- бизнес-логику задач, проектов и календарей;
- импорт и экспорт;
- другие экраны массовыми изменениями;
- способ хостинга.

---

# 8. Что отложено на потом

После успешной проверки Project Cards v1:

- распространить дизайн-систему на другие основные экраны;
- отдельно решить перенос импорта/экспорта из постоянного footer в настройки;
- при необходимости улучшить глобальный поиск и сервисное меню.

Отдельно по необходимости:

- перевод резервной копии импорта на Data Store;
- проверка импорта и восстановления;
- сервер и синхронизация между устройствами только при появлении реальной потребности.

---

# Контрольная точка

Стабильная версия:

**Architecture v2 + Data Model v1 + Data Store v1 + Application UX navigation v1 + Home Today dashboard v1 + Design System v1 task density + Task Card Unified v1 + Top Bar & Quick Actions v1 + Project Cards v1 находятся в `main`.**

Текущий этап:

**пользовательская визуальная проверка Project Cards v1.**

Следующий шаг:

**проверить опубликованный экран «Проекты»; затем выбрать следующий ключевой экран дизайн-системы.**
