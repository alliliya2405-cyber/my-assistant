QA-REPORT — МОЙ АССИСТЕНТ
Дата: 2026-08-05
Версия данных после normalize(): 8

ПРОВЕРКА V13

PASS — раздел «Сравнение дорожных карт» отсутствует в меню и таблице маршрутов.
PASS — сохранённая старая ссылка на этот раздел безопасно открывает «Месяц».
PASS — календарная сетка использует семь равных адаптивных столбцов без
минимальной ширины и обязательной горизонтальной прокрутки.
PASS — добавлены показатели «Всего», «Осталось», «Выполнено» и «Важные».
PASS — сегодня и выбранный день имеют отдельные визуальные состояния.
PASS — выбор дня открывает полный список задач в боковой панели.
PASS — в ячейке показываются первые две задачи и корректный счётчик остальных.
PASS — мобильная раскладка использует две колонки и не показывает дни соседних месяцев.
PASS — добавление, редактирование, завершение и удаление задач сохранены.
PASS — JavaScript проходит синтаксическую проверку.

ИСТОРИЯ: ПРОВЕРКА V12

PASS — раздел «Pomodoro» отсутствует в меню и таблице маршрутов.
PASS — сохранённая старая ссылка на Pomodoro безопасно открывает «Неделю».
PASS — экран «Неделя» выводит ровно семь вкладок дней и одну панель выбранного дня.
PASS — старая горизонтальная доска из семи длинных колонок удалена.
PASS — переключение дня вкладкой и стрелками работает; воскресенье и понедельник
корректно переводят пользователя на соседнюю неделю.
PASS — добавление, редактирование, завершение и удаление задач остаются доступны.
PASS — JavaScript проходит синтаксическую проверку.
PASS — автоматический smoke-test DOM подтверждает активную вкладку и смену дня.
NOTE — визуальная браузерная проверка была недоступна в текущей среде;
геометрия дополнительно защищена адаптивными CSS-правилами.

ИСТОРИЯ: ПРОВЕРКА V11

PASS — раздел «Процесс Числумиков» удалён из навигации и загрузочных файлов.
PASS — старые автоматически созданные задачи бизнес-процесса удаляются при миграции.
PASS — экран «Неделя» создаёт семь дневных колонок и четыре показателя сводки.
PASS — добавлены переход к дню, предыдущая и следующая неделя, возврат к текущей неделе.
PASS — мобильная раскладка не требует горизонтальной прокрутки.
PASS — JavaScript проходит синтаксическую проверку.
NOTE — ручная визуальная проверка в реальном браузере не заявляется.

ДОПОЛНИТЕЛЬНАЯ ПРОВЕРКА V9

PASS — проверены координаты заголовков A3:P3 листа
«Ч.2_Реестр операций».
PASS — все 16 Excel-столбцов выведены отдельно и в исходном порядке.
PASS — интерфейс не объединяет «ID + этап», «операцию + подпроцесс» или
«начало + конец + норматив».
PASS — у каждой строки 16 ячеек с подписями, совпадающими с заголовками Excel.

АВТОМАТИЧЕСКИЕ ПРОВЕРКИ

PASS — node --check app.js.
PASS — 26 маршрутов открыты в JSDOM без исключений:
Панель, День, Неделя, Месяц, Pomodoro, Проекты, Сравнение дорожных карт,
Задачи, Kanban, Команда, Ресурсная карта, Совещания, Отчёты,
Google Calendar, Здоровье, Семья, Образование, Досуг, Цитаты дня,
Заметки, Библиотека, Рефлексия, Доска, Поиск, Хронология,
Шаблоны и настройки.
PASS — функции из route map определены; renderBoard существует.
PASS — проверка кнопок на отрисованных маршрутах: click-обработчики или
нативное действие формы присутствуют. Бывшая кнопка drag-handle без
click-обработчика заменена семантическим маркером перетаскивания.
PASS — миграция старых meetings.questions/tasks в agenda/actionItems.
PASS — миграция старых assignments.person/task/note и статуса in_progress.
PASS — добавление, редактирование и удаление цитаты через форму.
PASS — добавление, редактирование и удаление совещания через форму.
PASS — создание поручения и задачи из строки совещания.
PASS — изменение строки совещания обновляет связанные записи без дубликатов.
PASS — удаление совещания удаляет связанные поручения и задачи.
PASS — повторная синхронизация сохраняет linkedKey и ID связанных задач.
PASS — JSON-сериализация состояния, запись в localStorage и нормализация
результата обратного чтения.
PASS — структура импорта проверяет объект и ограничивает файл размером 20 МБ;
при ошибке предусмотрено восстановление состояния beforeImport.
PASS — внешние CDN отсутствуют.
PASS — CSS содержит адаптивные правила для ширин 900, 820, 800, 760, 720,
640, 620 и 560 пикселей.
PASS — модальные формы используют одну прокручиваемую .modal-body, а footer с
кнопками остаётся отдельным и закреплённым; mobile height использует 100dvh.

ПРОВЕРКИ, КОТОРЫЕ НЕ ЗАМЕНЕНЫ АВТОМАТИКОЙ

NOT RUN IN REAL BROWSER — визуальная проверка всех экранов на конкретных
размерах телефона и ноутбука, touch-жесты, реальный drag-and-drop проектов,
рисование и перемещение на доске, звуковой и системный сигнал Pomodoro.
NOT RUN IN REAL BROWSER — фактическое скачивание экспортов JSON/ICS/.doc и
выбор локального файла в системном диалоге импорта. Логика обработчиков
проверена статически и в JSDOM, но системные диалоги там не воспроизводятся.
NOT RUN — двусторонняя OAuth-синхронизация Google Calendar, потому что она не
реализована в продукте и требует отдельной серверной/OAuth-инфраструктуры.

Попытка открыть локальную сборку в облачном Chrome была заблокирована для
localhost окружением браузера. Поэтому этот отчёт не утверждает, что выполнена
полная ручная визуальная проверка.

ИТОГ

Подтверждённые дефекты аудита исправлены. Автоматические проверки проходят.
Перед рабочим использованием рекомендуется короткий ручной smoke-test в
целевом Chrome/Safari/Edge на ноутбуке и телефоне.
UX-ОБНОВЛЕНИЕ — ПОВТОРНАЯ ПРОВЕРКА

Автоматически подтверждено:
- app.js проходит node --check;
- 25 видимых маршрутов открываются без ошибок (Kanban теперь является видом раздела «Задачи»);
- у кнопок на проверенных маршрутах есть обработчики;
- миграция старых данных, CRUD цитат и совещаний, localStorage и синхронизация без дубликатов проходят;
- идентификаторы связанных задач остаются стабильными.

Ручная проверка в реальном браузере по-прежнему требуется для drag-and-drop, загрузки/скачивания файлов, мобильной геометрии и визуального контроля длинных форм.

ОБНОВЛЕНИЕ НЕДЕЛИ — ПОВТОРНАЯ ПРОВЕРКА

Автоматически подтверждено:
- app.js проходит node --check;
- недельный экран выводит 7 заголовков дней и 7 карточек обзора;
- недельная сетка использует семь адаптивных столбцов без минимальной ширины 190 px;
- горизонтальная прокрутка недельной сетки отключена;
- кнопки дней открывают подробный экран соответствующего дня;
- добавление, редактирование и завершение задач остаются доступны.
- обычные задачи можно удалить из календарной карточки на экранах «День», «Неделя» и «Месяц»;
- удаление требует подтверждения, сохраняет состояние и перерисовывает экран;
- для автоматически связанных задач кнопка удаления не показывается, поскольку их жизненным циклом управляет исходная запись.

Ручная визуальная проверка на конкретных размерах экрана по-прежнему рекомендуется.

РАСПОЗНАВАНИЕ ЗАДАЧ СОВЕЩАНИЙ — ПРОВЕРКА

Автоматически подтверждено:
- app.js проходит node --check;
- 26 функций маршрутов отрисовываются в JSDOM без исключений (Kanban остаётся совместимым маршрутом и видом раздела «Задачи»);
- естественная фраза «Королева должна подготовить презентацию до 15 августа 2026» распознаётся как одна задача;
- распознаются исполнитель по внутреннему ID, текст задачи и дата 2026-08-15;
- окно подтверждения открывается кнопкой «Распознать задачи»;
- подтверждённая строка создаёт одно поручение и одну связанную календарную задачу без дубля;
- повторная синхронизация не создаёт дополнительных поручений или задач;
- normalize() мигрирует старые meetings.tasks и создаёт confirmedActions; версия данных становится 5;
- строгий формат через | сохранён для обратной совместимости.

Ограничения распознавания:
- алгоритм локальный и основан на правилах, поэтому спорные формулировки намеренно не распределяются автоматически;
- фамилия должна однозначно совпадать с участником, заранее добавленным в команду проекта или подпроекта;
- однофамильцы требуют ручного выбора в окне подтверждения;
- ручная визуальная проверка формы подтверждения и мобильной прокрутки в реальном браузере не выполнена и рекомендуется перед рабочим использованием.

ИНТЕГРАЦИЯ БИЗНЕС-ПРОЦЕССА «ЧИСЛУМИКИ» — ПРОВЕРКА

Автоматически подтверждено:
- app.js и chislumiki-process-data.js проходят node --check;
- 27 маршрутов, включая «Процесс Числумиков», отрисовываются без исключений;
- импортировано 6 этапов, 55 операций и 9 строк матрицы RACI;
- создаются ровно 55 связанных задач процесса;
- повторная синхронизация сохраняет ID и не создаёт дубликаты;
- изменение статуса операции обновляет связанную задачу;
- изменение статуса связанной задачи обновляет исходную операцию;
- у ответственных сотрудников в разделе «Команда» отображаются операции;
- normalize() мигрирует состояние версии 5 в версию 6 и добавляет процесс,
  не удаляя существующие проекты, задачи, совещания и пользовательские данные.

Ограничения и замечания источника:
- исходная таблица содержит две позиции линейки с номером 8;
- у позиции «Графические диктанты» плановая дата окончания раньше даты начала;
- в реестре операций есть отмеченные самим автором замечания к нумерации
  20.1 / 22.2 / 23.3 и несколько текстовых опечаток;
- эти неоднозначности не исправлялись молча: данные и комментарии источника
  сохранены, чтобы решение принял владелец процесса.

Ручная визуальная проверка длинных таблиц, горизонтальной прокрутки RACI и
мобильной геометрии в реальном браузере не выполнена. Рекомендуется короткий
smoke-test после распаковки архива.

ПЕРЕРАБОТКА ЧИТАЕМОСТИ РАЗДЕЛА «ПРОЦЕСС ЧИСЛУМИКОВ»

Автоматически подтверждено:
- app.js проходит node --check;
- все 27 маршрутов отрисовываются без исключений;
- после изменения разметки остаются 6 этапов, 55 операций и 55 связанных задач;
- поиск и фильтры сохраняют прежние идентификаторы элементов;
- у каждого поля статуса операции есть доступное название;
- мобильные правила переводят строки операций в вертикальные карточки;
- широкая таблица RACI намеренно сохраняет табличный вид и прокрутку, так как
  преобразование матрицы в карточки разрушило бы сопоставление ролей.

Визуальная оценка в реальном браузере в этой среде не выполнялась. Рекомендуется
ручной просмотр раздела на ширинах около 1440, 768 и 390 px.

СВЕРКА ПОДРАЗДЕЛА «ОПЕРАЦИИ» С ИСХОДНЫМ EXCEL

Автоматически подтверждено:
- считаны все 55 строк листа «Ч.2_Реестр операций»;
- для каждой строки сопоставлены 16 полей: ID, этап, подпроцесс, операция,
  результат, ответственный, участники, утверждающий, предшественник, начало,
  окончание, норматив, статус, версия/ссылка, критерий готовности и риск;
- проверено 880 значений; расхождений между Excel и данными приложения нет;
- роли «Ответственный», «Участники» и «Утверждает» выведены раздельно;
- начало, окончание и норматив показаны раздельно; пустые даты не заменяются
  предположениями;
- фильтр по ответственному присутствует, поиск охватывает все три ролевых поля;
- normalize() повышает версию данных с 6 до 7, сохраняет изменённый
  пользователем статус и заменяет устаревшие роли/сроки значениями Excel;
- 27 маршрутов открываются без исключений, 55 связанных задач сохраняют
  стабильные ID, синхронизация статусов работает в обе стороны;
- app.js и chislumiki-process-data.js проходят синтаксическую проверку.

Важно: в исходном Excel плановые даты заполнены только у первых 15 операций;
у остальных 40 строк даты действительно пусты. Приложение теперь показывает
это явно. Ручная визуальная проверка в реальном браузере не выполнялась.

ЕДИНАЯ КОМПАКТНАЯ ТИПОГРАФИКА — ПРОВЕРКА v10

Автоматически подтверждено:
- общая шкала типографики применяется после всех старых локальных правил и
  поэтому охватывает каждый раздел приложения;
- основной текст на ноутбуке — 14 px, заголовок раздела — 22 px, заголовки
  карточек — 15–18 px, элементы управления — 13 px;
- на экранах до 680 px применяется отдельная мобильная шкала 13–19 px;
- уменьшены отступы меню, верхней панели, карточек, форм, таблиц, Kanban,
  проектов, отчётов и процесса без изменения HTML-разметки и данных;
- все 27 маршрутов по-прежнему отрисовываются, 55 операций и 16 столбцов
  сохраняются, двусторонняя синхронизация статусов работает;
- app.js и chislumiki-process-data.js проходят синтаксическую проверку.

Ручная визуальная проверка в реальном браузере не выполнялась. Рекомендуется
после распаковки проверить масштаб браузера 100% на ноутбуке и телефоне.

ФИЛЬТРЫ РАЗДЕЛА «МЕСЯЦ» — ПРОВЕРКА v14

Автоматически подтверждено:
- фраза «Дорожная карта по проекту числумики» отсутствует в файлах приложения;
- четыре показателя сводки являются кнопками с состоянием aria-pressed;
- «Всего задач» показывает все задачи месяца;
- «Осталось» показывает только незавершённые задачи;
- «Выполнено» показывает только завершённые задачи;
- «Важное» показывает незавершённые приоритетные и срочные задачи;
- выбранный фильтр применяется к календарным ячейкам и панели выбранного дня;
- доступен сброс фильтра, пустая выборка сопровождается понятным сообщением;
- app.js проходит синтаксическую проверку.
- Версия 15: при открытии «Месяца» автоматически удаляется устаревший заголовок «Дорожная карта по проекту Числумики», включая вариант со старыми кавычками из сохранённого состояния браузера.
- Для стилей и JavaScript добавлена версия `v=15`, чтобы браузер не использовал прежнюю копию файлов.
- Проверено: синтаксис JavaScript, 42 ячейки месяца, четыре фильтра сводки и автоматическое удаление устаревшего заголовка — PASS.
Версия 16
- Проверено отображение панели действий в карточках раздела «Неделя».
- Высота области действий: 34 px в актуальном недельном представлении и 30 px в совместимом компактном представлении.
- Кнопка завершения имеет видимую зелёную галочку, контрастный фон и минимальную область нажатия 44 px.
